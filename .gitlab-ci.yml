image: node

cache:
  paths:
    - node_modules/

stages:
  - deploy

pages:
  stage: deploy
  script:
    - npm install
    - npm run build
    - rm -rf ./public
    - mv ./out ./public
  after_script:
    - rm -rf out
  artifacts:
    paths:
    - public
  only:
    - master

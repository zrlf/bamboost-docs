"use client";

import Layout from "@/fumapy/layout/layout";
// import { sources } from "@/fumapy/lib/source.api";
// import { ReactNode } from "react";
// import { usePathname } from "next/navigation";

// export default function Layout({ children }: { children: ReactNode }) {
//   const pathname = usePathname();
//   const firstSlug = pathname.split("/")[1];
//
//   const getSource = () => {
//     return Object.values(sources).find((source) => {
//       return source.baseUrl === firstSlug;
//     })
//   };
//
//   return <ApiLayout source={getSource()}>{children}</ApiLayout>;
// }
//

export default Layout;
